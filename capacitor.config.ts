import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.go-to-bikes',
  appName: 'Go To Bikes',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
